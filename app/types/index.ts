enum CharacterType {
    SAPIENS,
    ZOMBICUS,
}

interface IPoint {
    x: number;
    y: number;
}

interface ISize {
    width: number;
    height: number;
}

export {
    CharacterType,
    IPoint,
    ISize,
};
